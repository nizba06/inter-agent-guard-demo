.venv/
__pycache__/
*.py[cod]
.pytest_cache/
audit.jsonl
audit_*.jsonl
*.seal
# Local audit / debug logs from demo runs
[a-z].jsonl
x.jsonl
# Editable install metadata
*.egg-info/
# ONNX weights (~540 MB) — download via install_model.py, not committed
*.onnx
models-tmp/
# Secrets and local credentials — never commit
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json
.pypirc
